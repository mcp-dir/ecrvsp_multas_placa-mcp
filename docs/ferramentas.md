# Ferramentas

ECRVSP Multas RENAINF: Por Placa expõe 1 ferramenta (todas somente leitura).

### 1. `ecrvsp_multas_placa_consultar`
**Input**: `a3`, `a3_pin`, `login_cpf`, `login_senha`, `placa`, `exigibilidade` (opcional), `data_inicio` (opcional), `data_fim` (opcional)

ECRVSP Multas RENAINF: Por Placa, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar ECRVSP Multas RENAINF: Por Placa
```
